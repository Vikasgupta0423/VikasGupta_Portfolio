import SceneWrapper from "@/components/common/SceneWrapper";
import Hero from "@/components/hero/Hero";

const HeroScene = () => {
  return (
    <SceneWrapper id="home">

      <Hero />

    </SceneWrapper>
  );
};

export default HeroScene;